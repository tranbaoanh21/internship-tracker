{
  "name": "internship-application-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c cyan,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "test": "npm run test -w backend && npm run test -w frontend",
    "test:unit": "npm run test:unit -w backend && npm run test -w frontend",
    "build": "npm run build -w frontend",
    "db:migrate": "npm run db:migrate -w backend",
    "db:migrate:test": "cross-env NODE_ENV=test DB_PORT=3307 DB_NAME=internship_tracker_test npm run db:migrate -w backend",
    "db:seed": "npm run db:seed -w backend",
    "test:e2e": "playwright test",
    "db:backup": "scripts/backup-db.sh",
    "db:restore:drill": "scripts/dr-drill.sh"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "concurrently": "10.0.5",
    "cross-env": "10.1.0"
  },
  "engines": {
    "node": ">=24"
  },
  "allowScripts": {
    "argon2@0.45.1": true,
    "@scarf/scarf": false,
    "msgpackr-extract": false,
    "fsevents": false
  }
}
