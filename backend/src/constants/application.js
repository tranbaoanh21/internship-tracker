export const APPLICATION_STATUSES = [
  'wishlist',
  'applied',
  'interview',
  'offer',
  'rejected',
];

export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 100;

export const APPLICATION_ATTENTION_FILTERS = ['overdue', 'today', 'next7', 'none'];
export const APPLICATION_SORTS = ['updatedAt', 'followUpAt', 'appliedAt', 'company'];
export const APPLICATION_VIEWS = ['active', 'archived'];
